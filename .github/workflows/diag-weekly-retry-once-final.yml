name: Weekly Test Retry Once Final

on:
  push:
    branches:
      - diag/weekly-retry-once-final-2026-08-29

permissions:
  contents: read

jobs:
  retry-once:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    env:
      EXPECTED_SHA: 78bb290a0f08ec2b84ee995ba47bee18c1e492f3
      LIVE_BASE: https://cerberus-forge-deploy-backend.onrender.com
      KNOWN_ADMIN_USER_ID: "1976526372"
      TELEGRAM_ADMIN_CHAT_ID: ${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}
      CAMPAIGN_ID: f49934b9-86e5-4314-85d0-5387e1ed5b99
      TELEGRAM_MESSAGE_ID: "1139"
      FIXED_UPDATE_ID: "2147483001"
    steps:
      - name: Fail closed unless exact self-heal backend is live
        shell: bash
        run: |
          set -euo pipefail
          health="$(curl --fail-with-body --silent --show-error --max-time 20 "$LIVE_BASE/health")"
          status="$(printf '%s' "$health" | jq -r '.status // empty')"
          version="$(printf '%s' "$health" | jq -r '.version // empty')"
          echo "HEALTH_STATUS=$status"
          echo "LIVE_VERSION_SHA=$version"
          [ "$status" = "ok" ]
          [ "$version" = "$EXPECTED_SHA" ]

      - name: Fail closed unless Telegram runtime is healthy and admin matches
        shell: bash
        run: |
          set -euo pipefail
          [ -n "${TELEGRAM_ADMIN_CHAT_ID:-}" ]
          [ "$TELEGRAM_ADMIN_CHAT_ID" = "$KNOWN_ADMIN_USER_ID" ]
          body="$(curl --fail-with-body --silent --show-error --max-time 20 "$LIVE_BASE/api/telegram-status")"
          configured="$(printf '%s' "$body" | jq -r '.configured // false')"
          whitelist="$(printf '%s' "$body" | jq -r '.effectiveWhitelistConfigured // false')"
          webhook="$(printf '%s' "$body" | jq -r '.webhookConfigured // false')"
          webhook_match="$(printf '%s' "$body" | jq -r '.webhookMatchesExpectedUrl // false')"
          api_healthy="$(printf '%s' "$body" | jq -r '.apiHealthy // false')"
          backend_ready="$(printf '%s' "$body" | jq -r '.backendReady // false')"
          secret_configured="$(printf '%s' "$body" | jq -r '.secretConfigured // false')"
          backend_sha="$(printf '%s' "$body" | jq -r '.backendSha // empty')"
          echo "TELEGRAM_CONFIGURED=$configured"
          echo "TELEGRAM_WHITELIST_CONFIGURED=$whitelist"
          echo "TELEGRAM_WEBHOOK_CONFIGURED=$webhook"
          echo "TELEGRAM_WEBHOOK_MATCH=$webhook_match"
          echo "TELEGRAM_API_HEALTHY=$api_healthy"
          echo "TELEGRAM_BACKEND_READY=$backend_ready"
          echo "TELEGRAM_WEBHOOK_SECRET_CONFIGURED=$secret_configured"
          echo "TELEGRAM_BACKEND_SHA=$backend_sha"
          [ "$configured" = "true" ]
          [ "$whitelist" = "true" ]
          [ "$webhook" = "true" ]
          [ "$webhook_match" = "true" ]
          [ "$api_healthy" = "true" ]
          [ "$backend_ready" = "true" ]
          [ "$secret_configured" = "false" ]
          [ "$backend_sha" = "$EXPECTED_SHA" ]

      - name: Execute exactly one human-authorized retry callback
        shell: bash
        run: |
          set -euo pipefail
          payload="$(jq -nc \
            --argjson update_id "$FIXED_UPDATE_ID" \
            --argjson admin_id "$TELEGRAM_ADMIN_CHAT_ID" \
            --argjson message_id "$TELEGRAM_MESSAGE_ID" \
            --arg campaign_id "$CAMPAIGN_ID" \
            --argjson now "$(date +%s)" \
            '{
              update_id: $update_id,
              callback_query: {
                id: "cerberus-weekly-retry-once-final-20260829",
                from: { id: $admin_id, is_bot: false, first_name: "Admin" },
                message: {
                  message_id: $message_id,
                  date: $now,
                  chat: { id: $admin_id, type: "private" }
                },
                chat_instance: "cerberus-weekly-retry-once-final",
                data: ("campaign_weekly_retry_test:" + $campaign_id)
              }
            }')"
          response_file="$RUNNER_TEMP/weekly-retry-webhook.json"
          code="$(curl --silent --show-error --max-time 20 \
            -o "$response_file" -w '%{http_code}' \
            -H 'Content-Type: application/json' \
            --data-binary "$payload" \
            "$LIVE_BASE/api/telegram/webhook")"
          accepted="$(jq -r '.ok // false' "$response_file" 2>/dev/null || echo false)"
          echo "REAL_WEEKLY_RETRY_WEBHOOK_HTTP=$code"
          echo "REAL_WEEKLY_RETRY_WEBHOOK_ACCEPTED=$accepted"
          [ "$code" = "200" ]
          [ "$accepted" = "true" ]
          echo "RETRY_CALLBACK_SUBMITTED_ONCE=true"
          echo "CREATE_CAMPAIGN_REQUESTED_BY_WORKFLOW=false"
          echo "SENDNOW_REQUESTED_BY_WORKFLOW=false"
          echo "REAL_SUBSCRIBERS_TARGETED_BY_WORKFLOW=0"

      - name: Allow asynchronous retry handling to finish
        run: sleep 25

      - name: Verify backend stayed on exact live SHA
        shell: bash
        run: |
          set -euo pipefail
          health="$(curl --fail-with-body --silent --show-error --max-time 20 "$LIVE_BASE/health")"
          status="$(printf '%s' "$health" | jq -r '.status // empty')"
          version="$(printf '%s' "$health" | jq -r '.version // empty')"
          echo "POST_RETRY_HEALTH_STATUS=$status"
          echo "POST_RETRY_LIVE_VERSION_SHA=$version"
          [ "$status" = "ok" ]
          [ "$version" = "$EXPECTED_SHA" ]
